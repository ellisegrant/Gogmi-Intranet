/home
├── intranet-backend/          # Node.js Backend
│   ├── package.json          # Dependencies (express, cors, dotenv)
│   ├── .env                  # Environment variables (to be created)
│   └── server.js             # API server (to be created)
│
└── intranet-frontend/         # React Frontend  
    ├── src/
    │   ├── components/       # Reusable UI components
    │   ├── pages/
    │   │   └── Dashboard.jsx # ✅ Dashboard page (DONE!)
    │   ├── hooks/            # Custom React hooks
    │   ├── services/         # API calls
    │   ├── utils/            # Helper functions
    │   ├── assets/           # Images, icons
    │   ├── App.jsx           # ✅ Main app (UPDATED!)
    │   ├── main.jsx          # Entry point
    │   └── index.css         # ✅ Tailwind directives (DONE!)
    ├── tailwind.config.js    # ✅ Tailwind config (DONE!)
    ├── postcss.config.js     # ✅ PostCSS config (DONE!)
    └── package.json          # ✅ All dependencies installed!